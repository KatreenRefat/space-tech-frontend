# Used by `vite build`. GitHub Pages serves static files only, so API calls must
# go straight to the backend rather than through the dev proxy.
# This requires the backend to send CORS headers allowing the Pages origin:
#   https://sla7ly-org.github.io
VITE_API_BASE_URL=https://back.markwasfy00.xyz/api/v1
